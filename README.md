# cda-java

## variables

<pre><code>
int n = 12; 
</code></pre>
il faut préciser le type de la variable


## Scanner
Scanner sc=new Scanner(System.in);
int nb=sc.nextInt(); 

## reverse
resultat = nb%2+ resultat; mettre la postion du nb definira le sens d'écriture du resultat

## retour de valeur à une place précise
System.out.println(reverse.charAt(valeur));

## verifier l'égalité avec un string rajouter ! devant la var pour diff
if( var.equals("0000")){

}
